!!!
%html
  %head
    %title Deltafin
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .container
      .row
        .navbar
          .navbar-inner
            %a.brand{:href => root_path} Delta Trade
            %ul.nav
              %li
                %a{:href => root_path} Home
              %li
                %a{:href => page_path("about")} What is Delta Trade?
              - if user_signed_in?
                %li
                  = link_to "My Accounts", accounts_path
                %li
                  = link_to('Logout', destroy_user_session_path, :method => :delete)
              - else
                %li
                  = link_to('Login', new_user_session_path)
      .row
        %center
          - if alert
            %p.alert-message.warning.alert.alert-error= alert
          - if notice
            %p.alert-message.info.alert.alert-success= notice
      .row
        = yield
