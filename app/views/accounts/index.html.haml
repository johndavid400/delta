%h1.inline My Accounts
.new_acct_btn.pull-right= link_to 'New Account', new_account_path, :class => "btn btn-default inline"
%br
- if current_user.accounts.present?
  = render 'block'
- else
  You currently have no accounts
